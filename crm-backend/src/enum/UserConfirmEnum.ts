

export enum UserConfirmEnum{ PENDING = 'pending', EMAIL = 'email', APPROVAL = 'approval', DENIED = 'denied' };

// pending: anlamı: bekliyor
// email: anlamı: eposta
// approval: anlamı: onay
// denied: anlamı: reddedildi
